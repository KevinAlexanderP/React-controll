import { ACTION TYPES } from "../Actions/DCandidate"
export const dCandidate = (state = initialState.action ) =>{
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}